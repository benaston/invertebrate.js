function InvertebrateView(model) {
	"use strict";

	if (!(this instanceof InvertebrateView)) {
		return new InvertebrateView(); 
	}

	var that = this;

	
	that.constructTemplateUri = function() {
		
	};

	function init() {

		return that;
	}

	return init();
}
